# Service pointing at the previous Deployment. This will be the target for event
# consumption.
  kind: Service
  apiVersion: v1
  metadata:
    name: event-consumer
  spec:
    selector:
      app: event-consumer
    ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
